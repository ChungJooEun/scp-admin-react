const barGraphOptions = {
  scales: {
    y: {
      ticks: {
        beginAtZero: true,
        stepSize: 50,
      },
    },
  },
  // maintainAspectRatio: false,
};

export default barGraphOptions;
